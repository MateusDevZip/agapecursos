# Configuração para redirecionar requisições para o Passenger (Python Application)
Options +FollowSymLinks
RewriteEngine On
RewriteBase /

# Se o arquivo existir (imagens, css, js), serve o arquivo diretamente
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Caso contrário, redireciona tudo para o passenger_wsgi.py (sua app Flask)
RewriteRule ^(.*)$ passenger_wsgi.py/$1 [QSA,L]
